# Camoufox Browser Automation

Мощная платформа для автоматизации браузера на основе Camoufox с мультиаккаунтингом и полным функционалом Playwright.

## 🚀 Возможности

### Мультиаккаунтинг (как Dolphin/GoLogin)
- Создание уникальных профилей браузера с изолированными отпечатками
- Хранение cookies, localStorage и sessionStorage для каждого профиля
- Настройка прокси, user agent, viewport, timezone, locale и геолокации
- Управление профилями: создание, редактирование, удаление, клонирование
- Теги и группировка профилей

### Расширенный функционал Playwright
- **Управление cookies**: получение, установка, удаление cookies
- **Storage операции**: работа с localStorage и sessionStorage
- **Эмуляция геолокации**: настройка координат для каждой сессии
- **Работа с файлами**: загрузка файлов через формы
- **Генерация PDF**: сохранение страниц в PDF формат
- **Ожидание элементов**: wait_for_selector с таймаутами
- **События ввода**: эмуляция клавиатуры и мыши
- **Drag and drop**: перетаскивание элементов
- **Кастомизация**: user agents, viewport, timezone, locale

### API для n8n
- REST API с аутентификацией по ключам
- Создание и управление сессиями браузера
- Выполнение задач автоматизации
- Мониторинг статуса задач в реальном времени

## 🛠 Технологии

- **Frontend**: React 19, TypeScript, Tailwind CSS 4, Shadcn UI
- **Backend**: Node.js, Express, tRPC
- **Database**: MySQL/TiDB (через Drizzle ORM)
- **Browser Automation**: Camoufox (Firefox-based), Playwright
- **Authentication**: Manus OAuth

## 📦 Установка

```bash
# Клонировать репозиторий
git clone https://github.com/sergeevgit1/camoufox-automation.git
cd camoufox-automation

# Установить зависимости
pnpm install

# Установить Camoufox и Playwright
sudo pip3 install camoufox playwright
sudo python3.11 -m camoufox fetch

# Настроить базу данных
pnpm db:push

# Запустить dev сервер
pnpm dev
```

## 🎯 Быстрый старт

1. **Создать профиль браузера**
   - Перейдите в раздел "Профили"
   - Настройте уникальный отпечаток (ОС, локаль, прокси)
   - Сохраните профиль

2. **Создать сессию**
   - Перейдите в раздел "Сессии"
   - Выберите профиль (опционально)
   - Настройте параметры браузера
   - Создайте сессию

3. **Добавить задачи автоматизации**
   - Откройте сессию
   - Добавьте задачи: навигация, клики, скриншоты, JavaScript
   - Запустите выполнение

4. **Интеграция с n8n**
   - Создайте API ключ в разделе "API ключи"
   - Используйте REST API endpoints в n8n workflows

## 📚 API Endpoints

### Сессии
- `POST /api/sessions/create` - Создать сессию
- `GET /api/sessions/list` - Список сессий
- `DELETE /api/sessions/:id` - Удалить сессию

### Задачи
- `POST /api/tasks/create` - Создать задачу
- `GET /api/tasks/:sessionId` - Задачи сессии
- `GET /api/tasks/status/:taskId` - Статус задачи

### Профили
- `POST /api/profiles/create` - Создать профиль
- `GET /api/profiles/list` - Список профилей
- `PUT /api/profiles/:id` - Обновить профиль
- `DELETE /api/profiles/:id` - Удалить профиль

## 🔐 Аутентификация

Все API запросы требуют заголовок авторизации:

```bash
Authorization: Bearer YOUR_API_KEY
```

## 📝 Примеры использования

### Создание профиля

```javascript
const profile = await fetch('/api/profiles/create', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'Мой профиль',
    fingerprint: {
      os: 'windows',
      locale: 'ru-RU',
      timezone: 'Europe/Moscow',
      userAgent: 'Mozilla/5.0...',
      viewport: { width: 1920, height: 1080 }
    },
    proxy: {
      server: 'http://proxy.example.com:8080',
      username: 'user',
      password: 'pass'
    }
  })
});
```

### Создание задачи навигации

```javascript
const task = await fetch('/api/tasks/create', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    sessionId: 1,
    action: 'navigate',
    parameters: {
      url: 'https://example.com',
      wait_until: 'networkidle'
    }
  })
});
```

## 🤝 Вклад

Приветствуются pull requests! Для серьезных изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

MIT

## 🔗 Ссылки

- [Camoufox Documentation](https://camoufox.com/)
- [Playwright Documentation](https://playwright.dev/)
- [n8n Documentation](https://docs.n8n.io/)
