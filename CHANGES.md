# 🔄 Changelog: PocketBase Integration

## Дата: 2024-11-14

### ✨ Новые возможности

- **PocketBase аутентификация** - полная замена Manus OAuth
- **Email/Password вход** - встроенная аутентификация без внешних зависимостей
- **OAuth2 поддержка** - Google, GitHub, Facebook и другие провайдеры
- **Admin UI** - веб-интерфейс для управления пользователями (http://localhost:8090/_/)
- **Автоматическая инициализация схемы** - через PocketBase hooks
- **SQLite база данных** - замена MySQL, легче и быстрее

### 🗑️ Удалено

- Зависимость от Manus OAuth сервера
- MySQL контейнер и зависимости
- Переменные окружения: OAUTH_CLIENT_ID, OAUTH_CLIENT_SECRET, OAUTH_REDIRECT_URI
- Переменные окружения: DATABASE_URL, MYSQL_*
- Файлы: server/_core/oauth.ts, server/_core/sdk.ts

### 📝 Изменено

- `server/_core/index.ts` - использует новую систему аутентификации
- `server/_core/context.ts` - работает с PocketBase пользователями
- `docker-compose.yml` - PocketBase вместо MySQL
- `Dockerfile` - убраны зависимости MySQL
- `.env.example` - упрощенная конфигурация

### ➕ Добавлено

**Новые файлы:**
- `server/_core/auth.ts` - аутентификация через PocketBase
- `server/_core/pocketbase.ts` - PocketBase клиент
- `pocketbase/pb_hooks/main.pb.js` - автоматическая настройка схемы
- `pocketbase/pb_schema.js` - описание коллекций
- `README_POCKETBASE.md` - документация по PocketBase
- `MIGRATION_GUIDE.md` - руководство по миграции
- `CHANGES.md` - список изменений

**Новые зависимости:**
- `pocketbase` (npm package) - JavaScript SDK для PocketBase

### 🔧 Конфигурация

**Минимальный .env:**
```bash
PORT=6000
POCKETBASE_URL=http://pocketbase:8090
COOKIE_SECRET=your_secret_key
```

**Больше не нужны:**
- OAuth credentials
- MySQL credentials
- DATABASE_URL

### 🚀 Развертывание

**Было:**
```bash
docker compose up -d  # MySQL + App
# Настроить OAuth credentials
# Настроить DATABASE_URL
# Выполнить миграции: pnpm db:push
```

**Стало:**
```bash
docker compose up -d  # PocketBase + App
# Открыть http://localhost:8090/_/ и создать admin
# Готово! Схема создается автоматически
```

### 📊 Структура данных

**Коллекции PocketBase:**
- `users` - пользователи (встроенная auth коллекция)
- `profiles` - профили браузеров
- `sessions` - сессии автоматизации
- `tasks` - задачи автоматизации
- `apiKeys` - API ключи для n8n

Все коллекции создаются автоматически при первом запуске через hooks.

### 🔐 Безопасность

- JWT токены генерируются PocketBase
- API rules защищают данные пользователей
- Каждый пользователь видит только свои данные
- Admin имеет полный доступ через Admin UI

### 📚 Документация

- [README.md](./README.md) - обновленное основное руководство
- [README_DOCKER.md](./README_DOCKER.md) - Docker развертывание
- [README_POCKETBASE.md](./README_POCKETBASE.md) - PocketBase интеграция
- [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md) - миграция с OAuth

### ⚠️ Breaking Changes

1. **ID пользователей** - теперь UUID строки вместо чисел
2. **Даты** - ISO строки вместо Date объектов
3. **API endpoints** - новые endpoints для аутентификации:
   - `POST /api/auth/register` - регистрация
   - `POST /api/auth/login` - вход
   - `POST /api/auth/logout` - выход
   - `GET /api/auth/me` - текущий пользователь
4. **OAuth flow** - теперь через PocketBase OAuth2

### 🎯 Преимущества

✅ Нулевая конфигурация - работает из коробки  
✅ Автономность - нет внешних зависимостей  
✅ Простота - один контейнер вместо двух  
✅ Гибкость - множество OAuth2 провайдеров  
✅ Admin UI - удобное управление  
✅ Производительность - SQLite быстрее для малых нагрузок  

### 🔄 Миграция

Для миграции с предыдущей версии см. [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md)

### 🐛 Известные проблемы

Нет известных проблем на данный момент.

### 📞 Поддержка

- GitHub Issues: https://github.com/sergeevgit1/camoufox-automation/issues
- PocketBase Docs: https://pocketbase.io/docs/
